{
  "mcpServers": {
    "ouroboros": {
      "command": "uvx",
      "args": ["ouroboros-ai", "mcp", "serve"],
      "env": {
        "OUROBOROS_AGENTS_DIR": "${CLAUDE_PLUGIN_ROOT}/agents"
      }
    }
  }
}
