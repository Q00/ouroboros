[mcp_servers.ouroboros]
command = "uvx"
args = [
    "--python",
    ">=3.12",
    "--from",
    "ouroboros-ai[mcp]",
    "ouroboros",
    "mcp",
    "serve",
    "--runtime",
    "codex",
    "--llm-backend",
    "codex",
]
